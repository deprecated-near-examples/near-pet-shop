{
  "name": "near-pet-shop",
  "version": "1.0.0",
  "description": "Truffle's Pet Shop tutorial running on the NEAR EVM",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "local": "env NEAR_ENV=local parcel src/index.html",
    "betanet": "env NEAR_ENV=betanet parcel src/index.html",
    "testnet": "env NEAR_ENV=testnet parcel src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "near-web3-provider": "^1.0.5",
    "regenerator-runtime": "^0.13.7",
    "web3-eth-contract": "^1.3.0"
  }
}
